import Users from "../models/users";

class UsersService {
  static async create() {
    try {
    } catch (error) {
      throw error;
    }
  }

  static async getAll() {
    try {
    } catch (error) {
      throw error;
    }
  }
}

export default UsersService;
